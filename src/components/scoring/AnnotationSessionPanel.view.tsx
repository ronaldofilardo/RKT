type ViewProps = { sessionId: string; isActive: boolean; endorsedBy: string | null; onStart: () => void; onPause: () => void; onEnd: () => void; };

export function AnnotationSessionPanelView({ sessionId, isActive, endorsedBy, onStart, onPause, onEnd }: ViewProps) { return <div className="bg-white border-t border-gray-200 px-4 py-3"><div className="flex items-center justify-between"><div className="flex items-center gap-2"><SessionStatus isActive={isActive} /><EndorsementStatus isActive={isActive} endorsedBy={endorsedBy} /><span className="text-xs text-gray-500">Sessão: {sessionId.slice(0, 8)}...</span></div><SessionControls isActive={isActive} onStart={onStart} onPause={onPause} onEnd={onEnd} /></div></div>; }
function SessionStatus({ isActive }: { isActive: boolean }) { return <span className="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-semibold" style={{ backgroundColor: isActive ? '#dcfce7' : '#fef3c7', color: isActive ? '#166534' : '#92400e', borderColor: isActive ? '#86efac' : '#fcd34d' }}>{isActive ? '🔴 AO VIVO' : '⏸️ PAUSADA'}</span>; }
function EndorsementStatus({ isActive, endorsedBy }: { isActive: boolean; endorsedBy: string | null }) { if (isActive) return null; return endorsedBy ? <span className="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-xs font-semibold bg-green-100 text-green-700 border border-green-300">✅ Endossada por {endorsedBy}</span> : <span className="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-xs font-semibold bg-yellow-100 text-yellow-700 border border-yellow-300">⏳ Aguardando endosso</span>; }
function SessionControls({ isActive, onStart, onPause, onEnd }: { isActive: boolean; onStart: () => void; onPause: () => void; onEnd: () => void }) { return <div className="flex items-center gap-2">{isActive ? <button onClick={onPause} className="px-3 py-1.5 text-xs font-semibold rounded-lg bg-amber-100 text-amber-700 hover:bg-amber-200 transition-colors">Pausar</button> : <button onClick={onStart} className="px-3 py-1.5 text-xs font-semibold rounded-lg bg-sky-100 text-sky-700 hover:bg-sky-200 transition-colors">Retomar</button>}<button onClick={onEnd} className="px-3 py-1.5 text-xs font-semibold rounded-lg bg-gray-100 text-gray-600 hover:bg-gray-200 transition-colors">Encerrar</button></div>; }
