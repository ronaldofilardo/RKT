import type { ChangeEvent } from 'react';
import type { useVoiceRecorder } from '@/hooks/useVoiceRecorder';

export function NoteModeToggle({ mode, onText, onVoice }: { mode: 'text' | 'voice'; onText: () => void; onVoice: () => void }) { return <div className="flex rounded-xl bg-white/5 border border-white/10 p-0.5 mb-4"><button onClick={onText} className={`flex-1 py-2 rounded-lg text-sm font-bold transition-all ${mode === 'text' ? 'bg-blue-600 text-white shadow' : 'text-gray-400 hover:text-white'}`}>📝 Texto</button><button onClick={onVoice} className={`flex-1 py-2 rounded-lg text-sm font-bold transition-all ${mode === 'voice' ? 'bg-green-600 text-white shadow' : 'text-gray-400 hover:text-white'}`}>🎤 Voz</button></div>; }
export function TextNoteSection({ value, onChange }: { value: string; onChange: (e: ChangeEvent<HTMLTextAreaElement>) => void }) { return <div><textarea value={value} onChange={onChange} placeholder="Ex: jogador estava cansado, vento forte, etc." maxLength={500} rows={4} className="w-full px-3 py-2 rounded-xl bg-white/5 border border-white/15 text-white placeholder-gray-500 text-sm resize-none focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" /><p className="text-gray-500 text-xs text-right mt-1">{value.length}/500</p></div>; }
export function VoiceNoteSection({ recorder, formatDuration }: { recorder: ReturnType<typeof useVoiceRecorder>; formatDuration: (ms: number) => string }) { return <div className="flex flex-col items-center gap-3 py-2">{recorder.error && <div className="w-full rounded-xl bg-red-500/10 border border-red-500/30 p-3 text-center"><p className="text-red-400 text-xs font-semibold mb-1">{recorder.error}</p><p className="text-gray-400 text-[10px] leading-tight">Clique no cadeado na barra de endereço → Permissões → Microfone → Permitir.<br />Depois clique em <span className="text-white font-semibold">Tentar novamente</span>.</p></div>}{(recorder.state === 'idle' || recorder.error) && <button onClick={recorder.startRecording} className="w-full py-4 rounded-xl bg-green-600/20 border-2 border-green-500/50 text-green-400 font-bold hover:bg-green-600/30 transition-all flex items-center justify-center gap-2">Gravar nota de voz</button>}{recorder.state === 'recording' && <div className="w-full flex flex-col items-center gap-3"><div className="flex items-center gap-2 text-red-400"><span className="w-3 h-3 rounded-full bg-red-500 animate-pulse" /><span className="font-bold text-sm">Gravando...</span></div><p className="text-gray-300 text-lg font-mono">{formatDuration(recorder.durationMs)} / 0:15</p><button onClick={recorder.stopRecording} className="w-full py-3 rounded-xl bg-red-600 hover:bg-red-700 text-white font-bold transition-all">Parar gravação</button></div>}{recorder.state === 'recorded' && <div className="w-full flex flex-col items-center gap-3"><p className="text-gray-300 text-sm">Duração: {formatDuration(recorder.durationMs)}</p><div className="flex gap-2 w-full"><button onClick={recorder.playPreview} className="flex-1 py-2.5 rounded-xl bg-white/10 hover:bg-white/15 text-white font-bold transition-all text-sm">Replay</button><button onClick={recorder.clear} className="flex-1 py-2.5 rounded-xl bg-white/5 hover:bg-white/10 text-gray-400 font-bold border border-white/10 transition-all text-sm">Limpar</button></div></div>}</div>; }
export function NoteActions({ hasContent, onSave, onClear }: { hasContent: boolean | string; onSave: () => void; onClear: () => void }) { return <div className="flex flex-col gap-2 mt-4"><button onClick={onSave} className="w-full py-2.5 rounded-xl bg-blue-600 hover:bg-blue-700 text-white font-bold transition-all text-sm">{hasContent ? 'Guardar' : 'Fechar'}</button>{hasContent && <button onClick={onClear} className="w-full py-2.5 rounded-xl bg-transparent text-gray-400 font-bold border border-white/10 hover:bg-white/5 hover:text-white transition-all text-sm">Limpar observação</button>}</div>; }
