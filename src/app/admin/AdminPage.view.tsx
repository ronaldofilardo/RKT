import type { User } from './page';

const ROLES = ['ADMIN', 'GESTOR', 'COACH', 'ATHLETE', 'SPECTATOR'];
type Props = { error: string | null; showUsers: boolean; users: User[]; editingUser: string | null; newRole: string; onLogout: () => void; onToggleUsers: () => void; onRoleChange: (value: string) => void; onSaveRole: (id: string) => void; onCancelEdit: () => void; onStartEdit: (user: User) => void; onDelete: (id: string) => void; onBack: () => void; };

export function AdminPageView({ error, showUsers, users, editingUser, newRole, onLogout, onToggleUsers, onRoleChange, onSaveRole, onCancelEdit, onStartEdit, onDelete, onBack }: Props) { return <div className="min-h-screen bg-gray-50"><header className="bg-white shadow-sm border-b safe-top"><div className="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between gap-4"><h1 className="text-lg sm:text-2xl font-bold text-gray-900 truncate">Admin Dashboard</h1><div className="flex items-center gap-2 sm:gap-4 flex-shrink-0"><span className="hidden sm:inline text-sm text-gray-600">Administrador</span><button onClick={onLogout} className="text-sm text-gray-600 hover:text-gray-900 px-2 py-2 sm:px-0 sm:py-0">Sair</button></div></div></header><main className="max-w-7xl mx-auto px-4 py-6 sm:py-8">{error && <div className="mb-4 p-3 bg-red-50 text-red-700 rounded-lg text-sm">{error}</div>}<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6"><div className="bg-white p-4 sm:p-6 rounded-lg shadow-sm border"><h3 className="text-lg font-semibold mb-2">Gerenciar Usuários</h3><p className="text-gray-600 mb-4 text-sm sm:text-base">Administrar usuários e permissões</p><button onClick={onToggleUsers} className="w-full bg-sky-600 text-white py-3 rounded-lg hover:bg-sky-700 active:scale-[0.98] transition-transform">{showUsers ? 'Fechar' : 'Gerenciar'}</button></div><div className="bg-white p-4 sm:p-6 rounded-lg shadow-sm border"><h3 className="text-lg font-semibold mb-2">Configurações</h3><p className="text-gray-600 mb-4 text-sm sm:text-base">Configurações do sistema</p><button className="w-full bg-gray-300 text-gray-500 py-3 rounded-lg cursor-not-allowed">Em breve</button></div></div>{showUsers && <UsersTable users={users} editingUser={editingUser} newRole={newRole} onRoleChange={onRoleChange} onSaveRole={onSaveRole} onCancelEdit={onCancelEdit} onStartEdit={onStartEdit} onDelete={onDelete} />}<div className="mt-6 sm:mt-8"><button onClick={onBack} className="text-sky-600 hover:text-sky-700">← Voltar para Dashboard</button></div></main></div>; }
function roleClass(role: string) { return role === 'ADMIN' ? 'bg-purple-100 text-purple-700' : role === 'COACH' ? 'bg-blue-100 text-blue-700' : role === 'GESTOR' ? 'bg-orange-100 text-orange-700' : 'bg-gray-100 text-gray-700'; }
function UsersTable({ users, editingUser, newRole, onRoleChange, onSaveRole, onCancelEdit, onStartEdit, onDelete }: Omit<Props, 'error' | 'showUsers' | 'onLogout' | 'onToggleUsers' | 'onBack'>) { return <div className="mt-6 sm:mt-8 bg-white rounded-lg shadow-sm border overflow-hidden"><div className="px-4 sm:px-6 py-4 border-b bg-gray-50"><h3 className="text-lg font-semibold">Usuários Cadastrados</h3></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-gray-50 text-gray-600"><tr>{['Nome', 'Email', 'Role', 'Clube', 'Ações'].map((label) => <th key={label} className="text-left px-4 sm:px-6 py-3 font-medium">{label}</th>)}</tr></thead><tbody className="divide-y">{users.map((user) => <UserRow key={user.id} user={user} editingUser={editingUser} newRole={newRole} onRoleChange={onRoleChange} onSaveRole={onSaveRole} onCancelEdit={onCancelEdit} onStartEdit={onStartEdit} onDelete={onDelete} />)}</tbody></table></div></div>; }
function UserRow({ user, editingUser, newRole, onRoleChange, onSaveRole, onCancelEdit, onStartEdit, onDelete }: { user: User; editingUser: string | null; newRole: string; onRoleChange: (value: string) => void; onSaveRole: (id: string) => void; onCancelEdit: () => void; onStartEdit: (user: User) => void; onDelete: (id: string) => void }) { const editing = editingUser === user.id; return <tr className="hover:bg-gray-50"><td className="px-4 sm:px-6 py-3">{user.name}</td><td className="px-4 sm:px-6 py-3 text-gray-500">{user.email}</td><td className="px-4 sm:px-6 py-3">{editing ? <select value={newRole} onChange={(e) => onRoleChange(e.target.value)} className="border rounded px-2 py-1 text-xs">{ROLES.map((role) => <option key={role} value={role}>{role}</option>)}</select> : <span className={`px-2 py-0.5 rounded text-xs font-semibold ${roleClass(user.role)}`}>{user.role}</span>}</td><td className="px-4 sm:px-6 py-3 text-gray-500">{user.club || '-'}</td><td className="px-4 sm:px-6 py-3 text-right whitespace-nowrap">{editing ? <><button onClick={() => onSaveRole(user.id)} className="text-xs text-green-600 hover:underline">Salvar</button><button onClick={onCancelEdit} className="text-xs text-gray-500 hover:underline ml-2">Cancelar</button></> : <><button onClick={() => onStartEdit(user)} className="text-xs text-sky-600 hover:underline">Alterar Role</button><button onClick={() => onDelete(user.id)} className="text-xs text-red-600 hover:underline ml-2">Excluir</button></>}</td></tr>; }
