import type { Match } from './page';
export function filterMatches(matches:Match[],searchTerm:string,filterStatus:string,filterTournament:string){const term=searchTerm.toLowerCase();return matches.filter(match=>{const p1=match.player1?.name||'';const p2=match.player2?.name||'';const tournament=match.tournamentName||'';const search=p1.toLowerCase().includes(term)||p2.toLowerCase().includes(term)||tournament.toLowerCase().includes(term);const status=filterStatus==='ALL'||match.state===filterStatus;const tournamentMatch=!filterTournament||tournament===filterTournament;return search&&status&&tournamentMatch;});}
